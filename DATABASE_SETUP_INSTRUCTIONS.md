# 📋 تعليمات إعداد قاعدة البيانات الإنتاجية

## ⚠️ المشكلة الحالية

**الخطأ:** "فشل في تحميل الإيرادات" و "فشل في تحميل المصروفات"

**السبب:** قاعدة البيانات الإنتاجية لا تحتوي على جداول `revenues` و `expenses` بعد.

**الحل:** تطبيق Migration يدوياً عبر Cloudflare Dashboard.

---

## 🔧 خطوات الحل (5 دقائق فقط)

### الطريقة 1: عبر Cloudflare Dashboard (الأسهل) ✅

#### الخطوة 1: افتح Cloudflare Dashboard
1. اذهب إلى: https://dash.cloudflare.com
2. سجل الدخول بحسابك

#### الخطوة 2: افتح قاعدة البيانات
1. من القائمة اليسرى، اختر **Workers & Pages**
2. اضغط على **D1 SQL Database**
3. اختر قاعدة البيانات: **highlevel-nursery-production**

#### الخطوة 3: افتح Console
1. في صفحة قاعدة البيانات، اضغط على تبويب **Console**
2. ستظهر لك مربع نص لإدخال SQL commands

#### الخطوة 4: نسخ ولصق الكود
1. افتح الملف: `PRODUCTION_DATABASE_SETUP.sql`
2. انسخ **كامل** المحتوى
3. الصق المحتوى في مربع Console
4. اضغط **Execute** أو **Run**

#### الخطوة 5: التحقق من النجاح
إذا ظهرت رسالة نجاح، قم بتشغيل الأمر التالي للتحقق:

```sql
SELECT name FROM sqlite_master WHERE type='table' AND name IN ('revenues', 'expenses');
```

يجب أن ترى:
```
revenues
expenses
```

---

### الطريقة 2: عبر wrangler (إذا كان لديك صلاحيات API)

```bash
# تطبيق جميع migrations
cd /home/user/highlevel-nursery
npx wrangler d1 migrations apply highlevel-nursery-production --remote

# أو تنفيذ الملف مباشرة
npx wrangler d1 execute highlevel-nursery-production --remote --file=PRODUCTION_DATABASE_SETUP.sql
```

**ملاحظة:** هذه الطريقة قد لا تعمل إذا كان الـ API Token لا يملك صلاحيات D1.

---

## ✅ التحقق من النجاح

بعد تطبيق الـ SQL، قم بالتحقق:

### 1. افتح صفحة المديرة
https://highlevel-nursery.pages.dev/admin/nursery/login
- **كلمة المرور:** 1020
- اذهب إلى **إدارة الإيرادات**
- يجب أن ترى 3 إيرادات تجريبية (إجمالي 9,700 د.ك)

### 2. افتح صفحة مجلس الإدارة
https://highlevel-nursery.pages.dev/admin/board/login
- **PIN:** 5050
- اذهب إلى **إدارة المصروفات**
- يجب أن ترى 3 مصروفات تجريبية (إجمالي 7,300 د.ك)
- اذهب إلى **التقرير المالي الشامل**
- يجب أن ترى صافي الربح: 2,400 د.ك (24.7%)

---

## 📊 البيانات التجريبية

### الإيرادات (9,700 د.ك)
1. **اشتراكات شهر ديسمبر 2025** - 5,000 د.ك - 2025-12-01
2. **اشتراكات متأخرة** - 3,500 د.ك - 2025-12-05
3. **رسوم الأنشطة الإضافية** - 1,200 د.ك - 2025-12-10

### المصروفات (7,300 د.ك)
1. **إيجار المبنى - ديسمبر 2025** - 2,000 د.ك - 2025-12-01
2. **رواتب المعلمات - ديسمبر 2025** - 4,500 د.ك - 2025-12-01
3. **صيانة وإصلاحات** - 800 د.ك - 2025-12-08

### صافي الربح
- **الإيرادات:** 9,700 د.ك
- **المصروفات:** 7,300 د.ك
- **صافي الربح:** 2,400 د.ك (24.7%)

---

## 🗑️ حذف البيانات التجريبية (اختياري)

إذا أردت حذف البيانات التجريبية والبدء بقاعدة بيانات فارغة:

```sql
-- حذف جميع الإيرادات
DELETE FROM revenues;

-- حذف جميع المصروفات
DELETE FROM expenses;

-- إعادة ضبط العدادات
DELETE FROM sqlite_sequence WHERE name IN ('revenues', 'expenses');
```

---

## 🆘 استكشاف الأخطاء

### الخطأ: "table revenues already exists"
**الحل:** الجداول موجودة بالفعل. تجاهل هذا الخطأ أو استخدم `CREATE TABLE IF NOT EXISTS`.

### الخطأ: "UNIQUE constraint failed"
**الحل:** البيانات التجريبية موجودة بالفعل. يمكنك تجاهل هذا الخطأ أو حذف البيانات التجريبية أولاً.

### لا يمكن رؤية البيانات على الموقع
**الحل:**
1. تأكد من تطبيق الـ SQL على قاعدة البيانات **الإنتاجية** (وليست المحلية)
2. انتظر دقيقة واحدة ثم أعد تحميل الصفحة
3. امسح Cache المتصفح (Ctrl+Shift+R)

---

## 📞 الدعم

إذا واجهت أي مشكلة، تأكد من:
1. ✅ تسجيل الدخول بالبيانات الصحيحة
2. ✅ تطبيق الـ SQL على قاعدة البيانات الإنتاجية
3. ✅ إعادة تحميل الصفحة بعد التطبيق
4. ✅ مسح Cache المتصفح

---

## ✨ بعد التطبيق

بمجرد تطبيق الـ SQL، سيعمل النظام المالي بالكامل:
- ✅ صفحة إدارة الإيرادات (المديرة)
- ✅ صفحة إدارة المصروفات (مجلس الإدارة)
- ✅ التقرير المالي الشامل (مجلس الإدارة)
- ✅ جميع APIs (إضافة، تعديل، حذف)
- ✅ حساب صافي الربح تلقائياً

**الموقع:** https://highlevel-nursery.pages.dev

**تم بنجاح! 🎉**
