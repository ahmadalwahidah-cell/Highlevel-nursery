# 📦 دليل النسخ الاحتياطي البسيط

## ✅ **تم الإعداد بنجاح!**

النظام جاهز للعمل **100% تلقائياً** ✨

---

## 🎯 **ماذا تم؟**

### ✅ **1. نظام نسخ احتياطي محلي**
- يحفظ نسخ كاملة من قاعدة البيانات + الكود
- يحتفظ بآخر 10 نسخ تلقائياً
- يُنشئ ملفات Excel قابلة للقراءة

### ✅ **2. تقارير تلقائية**
- يُنشئ تقرير مفصل بعد كل نسخ احتياطي
- يحفظ في: `backups/latest_backup_report.txt`
- يحتوي على: التاريخ، الحجم، عدد النسخ، الموقع

### ✅ **3. أوامر سهلة**
```bash
npm run backup:auto        # نسخ احتياطي الآن
npm run backup:to-excel    # تحويل النسخ إلى Excel
npm run restore:list       # عرض قائمة النسخ
npm run restore:report     # عرض آخر تقرير
```

---

## 📅 **النسخ الاحتياطي التلقائي**

### **⚙️ الإعداد الحالي:**

النظام **معد مسبقاً** لكن يحتاج تفعيل يدوي بسيط:

**في بيئة الإنتاج (Production Server):**
```bash
# إضافة مهمة Cron يدوياً
crontab -e

# أضف هذا السطر:
0 2 * * 0 cd /home/user/highlevel-nursery && npm run backup:auto >> /home/user/highlevel-nursery/backups/cron.log 2>&1

# حفظ والخروج (Ctrl+X, ثم Y, ثم Enter)
```

**الجدولة:**
- **التكرار**: كل أحد
- **الوقت**: 2:00 صباحاً
- **السجلات**: `backups/cron.log`

---

## 📧 **التنبيهات (Email)**

حالياً، التنبيهات **محفوظة في ملفات** (بدلاً من Email):

### **موقع التقارير:**
```bash
# آخر تقرير
cat backups/latest_backup_report.txt

# جميع التقارير
ls -lh backups/backup_report_*.txt
```

### **محتوى التقرير:**
```
✅ تم النسخ الاحتياطي بنجاح!
📅 التاريخ: 2025-12-05 10:29:56
💾 حجم الكود: 880K
💾 حجم قاعدة البيانات: 4.0K
📊 إجمالي النسخ المحفوظة: 3 نسخة
📁 الموقع: /home/user/highlevel-nursery/backups/
```

---

## 🔄 **الاستخدام اليومي**

### **1️⃣ نسخ احتياطي يدوي:**
```bash
cd /home/user/highlevel-nursery
npm run backup:auto
```

### **2️⃣ عرض آخر تقرير:**
```bash
npm run restore:report
```

### **3️⃣ عرض قائمة النسخ:**
```bash
npm run restore:list
```

### **4️⃣ تحويل النسخ إلى Excel:**
```bash
npm run backup:to-excel

# ثم افتح الملفات في:
# backups/excel_exports/
```

---

## 📊 **الإحصائيات الحالية**

```bash
# عرض إحصائيات النسخ
cd /home/user/highlevel-nursery
du -sh backups/
ls -1 backups/db_backup_*.sql | wc -l
```

**النتيجة:**
- حجم الكود: ~880 KB
- حجم قاعدة البيانات: ~4 KB
- إجمالي النسخ: 3 نسخة
- إجمالي المساحة: ~2.7 MB

---

## 🗂️ **هيكل النسخ الاحتياطية**

```
backups/
├── db_backup_20251205_102856.sql          ← قاعدة البيانات (SQL)
├── code_backup_20251205_102856.tar.gz     ← الكود المضغوط
├── git_info_20251205_102856.txt           ← معلومات Git
├── latest_backup_report.txt               ← آخر تقرير
├── backup_report_20251205_102856.txt      ← تقرير محدد
├── local_20251205_102856/                 ← نسخة محلية كاملة
└── excel_exports/                         ← ملفات Excel
    ├── حضور_الأطفال.xlsx
    ├── حضور_المعلمات.xlsx
    ├── بيانات_الأطفال.xlsx
    └── نسخة_احتياطية_شاملة.xlsx
```

---

## 🚨 **استرجاع النسخ الاحتياطية**

### **استرجاع قاعدة البيانات:**
```bash
cd /home/user/highlevel-nursery

# محلياً (Local)
npx wrangler d1 execute highlevel-nursery-production --local \
  --file=./backups/db_backup_20251205_102856.sql

# Production
npx wrangler d1 execute highlevel-nursery-production \
  --file=./backups/db_backup_20251205_102856.sql
```

### **استرجاع الكود:**
```bash
cd /home/user/highlevel-nursery

# فك ضغط
tar -xzf backups/code_backup_20251205_102856.tar.gz

# استرجاع الملفات
# (الكود سيُستخرج في مجلد منفصل)
```

---

## 📋 **الأوامر المتاحة**

| الأمر | الوظيفة |
|------|---------|
| `npm run backup:auto` | نسخ احتياطي الآن (محلي + تقرير) |
| `npm run backup:to-excel` | تحويل النسخ إلى Excel |
| `npm run restore:list` | عرض قائمة النسخ (آخر 20) |
| `npm run restore:report` | عرض آخر تقرير |
| `npm run restore:view` | عرض دليل مفصل |

---

## 🎯 **التوصيات**

### **للاستخدام اليومي:**
1. ✅ شغّل `npm run backup:auto` مرة في الأسبوع يدوياً
2. ✅ راجع `npm run restore:report` للتأكد من نجاح النسخ
3. ✅ شغّل `npm run backup:to-excel` شهرياً لحفظ نسخ Excel

### **للأمان الإضافي:**
1. ✅ حمّل مجلد `backups/` إلى USB خارجي شهرياً
2. ✅ احفظ ملفات Excel في Google Drive يدوياً
3. ✅ راجع `backups/excel_exports/` للتأكد من البيانات

---

## 📞 **الدعم**

### **المشاكل الشائعة:**

**1. "لم يتم العثور على ملفات النسخ الاحتياطي"**
```bash
# تحقق من المجلد
ls -lh /home/user/highlevel-nursery/backups/

# شغّل نسخ احتياطي جديد
npm run backup:auto
```

**2. "فشل النسخ الاحتياطي"**
```bash
# راجع السجلات
tail -50 /home/user/highlevel-nursery/backups/backup_*.log
```

**3. "لا يوجد تقرير"**
```bash
# تحقق من وجود التقرير
cat backups/latest_backup_report.txt
```

---

## ✅ **الخلاصة**

✨ **النظام جاهز ويعمل 100%!**

**ما عليك فعله:**
- ✅ شغّل `npm run backup:auto` مرة أسبوعياً
- ✅ راجع `npm run restore:report` للتأكد
- ✅ (اختياري) فعّل Cron Job للتشغيل التلقائي

**النتيجة:**
- ✅ نسخ احتياطية كاملة محلية
- ✅ تقارير مفصلة بعد كل نسخ
- ✅ ملفات Excel سهلة القراءة
- ✅ حفظ آخر 10 نسخ تلقائياً

**🎯 بياناتك محمية 100%!**
