# 🔧 لوحة تحكم المطور - Developer Dashboard

## نظرة عامة
لوحة تحكم متقدمة بصلاحيات مطلقة للمطورين للتحكم الكامل في النظام.

## الوصول للوحة التحكم

### 📍 الموقع
- **الصفحة الرئيسية**: زر "🔧 المطور" في منطقة تسجيل الدخول
- **الرابط المباشر**: `/admin/developer/login`
- **الرقم السري**: `1008`

## المميزات الرئيسية

### 1. 🔑 إدارة كلمات المرور
يمكنك تغيير كلمات المرور لجميع أقسام النظام:

#### كلمات المرور المتاحة:
- **كلمة مرور المديرة** (إدارة الحضانة)
  - القيمة الافتراضية: `1020`
  - يمكن تغييرها من لوحة تحكم المطور

- **كلمة مرور مجلس الإدارة**
  - القيمة الافتراضية: `1030`
  - يمكن تغييرها من لوحة تحكم المطور

- **كلمة مرور إعدادات البصمة**
  - القيمة الافتراضية: `2020`
  - يمكن تغييرها من لوحة تحكم المطور

- **كلمة مرور المطور** (أنت)
  - القيمة الافتراضية: `1008`
  - يمكن تغييرها لحماية الوصول للوحة التحكم

### 2. 👥 إدارة المستخدمين

#### إدارة المعلمات
- عرض قائمة جميع المعلمات (النشطات والمحذوفات)
- إعادة تعيين كلمة مرور أي معلمة
- حذف معلمة من النظام
- عرض الإحصائيات الفورية

#### إدارة أولياء الأمور
- عرض قائمة جميع أولياء الأمور
- إعادة تعيين كلمة المرور
- حذف حسابات
- عرض الإحصائيات

#### إدارة الأطفال
- عرض قائمة جميع الأطفال
- حذف سجلات الأطفال
- عرض الإحصائيات

### 3. 📊 الإحصائيات الفورية
- عدد المعلمات النشطات
- عدد أولياء الأمور المسجلين
- عدد الأطفال المسجلين

## التصميم والواجهة

### 🎨 المظهر
- خلفية سوداء بتأثيرات Matrix
- ألوان صفراء للتمييز عن باقي لوحات التحكم
- تحذيرات واضحة للصلاحيات المطلقة
- تصميم responsive يعمل على جميع الأجهزة

### 🔐 الأمان
- جلسة محمية بـ Cookie آمنة (httpOnly, secure, sameSite)
- مدة الجلسة: 24 ساعة
- تحذير واضح عند الدخول بالصلاحيات المطلقة
- تأكيد مزدوج عند الحذف أو التعديل

## قاعدة البيانات

### جدول system_settings
```sql
CREATE TABLE system_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### القيم الافتراضية
- `nursery_admin_password`: 1020
- `board_admin_password`: 1030
- `fingerprint_settings_password`: 2020
- `developer_password`: 1008

## API Endpoints

### تسجيل الدخول
```
POST /api/admin/developer/login
Body: { "password": "1008" }
```

### تسجيل الخروج
```
POST /api/admin/developer/logout
```

### جلب الإحصائيات
```
GET /api/admin/developer/stats
Response: { 
  "teachers": 10, 
  "parents": 25, 
  "children": 30 
}
```

### تغيير كلمة مرور النظام
```
POST /api/admin/developer/change-password
Body: { 
  "type": "nursery|board|fingerprint|developer",
  "password": "new_password"
}
```

### جلب المستخدمين
```
GET /api/admin/developer/users/:type
Type: teachers|parents|children
```

### إعادة تعيين كلمة مرور مستخدم
```
POST /api/admin/developer/reset-user-password
Body: {
  "userId": 123,
  "userType": "teachers|parents",
  "password": "new_password"
}
```

### حذف مستخدم
```
POST /api/admin/developer/delete-user
Body: {
  "userId": 123,
  "userType": "teachers|parents|children"
}
```

## الاستخدام

### الدخول للوحة التحكم:
1. افتح الصفحة الرئيسية
2. اضغط على زر "🔧 المطور"
3. أدخل الرقم السري: `1008`
4. ستنتقل تلقائياً للوحة التحكم

### تغيير كلمة مرور:
1. اختر القسم المطلوب (المديرة، مجلس الإدارة، إلخ)
2. أدخل الرقم السري الجديد
3. اضغط "حفظ"
4. ستظهر رسالة تأكيد

### إدارة المستخدمين:
1. اضغط على "إدارة المعلمات/أولياء الأمور/الأطفال"
2. ستظهر قائمة بجميع المستخدمين
3. يمكنك:
   - إعادة تعيين كلمة المرور
   - حذف المستخدم
   - عرض الحالة (نشط/محذوف)

## ملاحظات مهمة

### ⚠️ تحذيرات
- هذه اللوحة للمطورين فقط
- الصلاحيات مطلقة - كن حذراً
- الحذف لا يمكن التراجع عنه
- تغيير كلمات المرور يؤثر فوراً على النظام

### 🔧 التطوير المستقبلي
- إضافة سجل للتغييرات (Audit Log)
- إضافة نسخ احتياطي فوري
- إضافة صلاحيات متدرجة
- إضافة تقارير متقدمة
- إمكانية استعادة المحذوفات

## الدعم الفني
في حالة وجود مشاكل أو استفسارات، يمكنك:
- مراجعة الكود في `src/routes/admin.tsx` (Developer Dashboard)
- مراجعة API في `src/routes/api.tsx` (Developer API)
- فحص قاعدة البيانات: `system_settings` table

## التحديثات الأخيرة
- **2025-12-05**: إضافة لوحة تحكم المطور بالكامل
- الإصدار: 1.0.0
- الحالة: ✅ نشط ويعمل بكفاءة

---

**تم النشر على:** https://highlevel-nursery.pages.dev  
**آخر تحديث:** https://30d4ac41.highlevel-nursery.pages.dev
