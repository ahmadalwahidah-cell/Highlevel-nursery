# 🔧 دليل صفحة المطور - الإصدار 3.1.0
## إدارة أوقات المعلمات المخصصة

**آخر تحديث:** 6 ديسمبر 2025  
**الإصدار:** 3.1.0

---

## 📋 نظرة عامة

تتيح هذه الميزة للمطور إمكانية تخصيص أوقات البصمة والتأخير لكل معلمة بشكل منفصل ومستقل، دون التأثير على الإعدادات العامة للمعلمات الأخريات.

---

## 🔐 الوصول إلى صفحة المطور

### الرابط المباشر
```
https://highlevel-nursery.pages.dev/developer/teacher-schedules?password=dev2025
```

### كلمة المرور
```
dev2025
```

### طريقة تسجيل الدخول
1. افتح الرابط أعلاه في المتصفح
2. أدخل كلمة المرور: `dev2025`
3. اضغط على زر "دخول"

**ملاحظة:** كلمة المرور تُحفظ في Cookie لمدة 24 ساعة، لن تحتاج لإدخالها مرة أخرى حتى انتهاء المدة.

---

## ⚙️ الإعدادات العامة (الافتراضية)

هذه الإعدادات تُطبَّق على جميع المعلمات التي **ليس لديهن** توقيت مخصص:

| الإعداد | الوقت الافتراضي | الوصف |
|--------|------------------|-------|
| **فتح بصمة الحضور** | 02:00 صباحاً | الوقت الذي تفتح فيه بصمة الحضور |
| **بدء احتساب التأخير** | 07:15 صباحاً | الوقت الذي يبدأ فيه احتساب دقائق التأخير |
| **فتح بصمة الانصراف** | 12:00 ظهراً (افتراضي) | الوقت الذي تفتح فيه بصمة الانصراف |

---

## 🎯 التوقيت المخصص لكل معلمة

يمكنك تحديد أوقات خاصة لكل معلمة على حدة:

### 1️⃣ فتح بصمة الحضور
- **الوصف:** الوقت الذي تستطيع فيه المعلمة عمل بصمة الحضور
- **مثال:** إذا حددت الساعة `06:00`، فإن المعلمة تستطيع بصمة الحضور من الساعة 6:00 صباحاً
- **إذا تركت الحقل فارغاً:** سيتم استخدام الإعداد العام (02:00 صباحاً)

### 2️⃣ بدء احتساب التأخير
- **الوصف:** الوقت الذي يبدأ فيه النظام باحتساب دقائق التأخير
- **مثال:** إذا حددت الساعة `08:00`، فإن أي حضور بعد هذا الوقت سيُحسب متأخراً
- **إذا تركت الحقل فارغاً:** سيتم استخدام الإعداد العام (07:15 صباحاً)

### 3️⃣ فتح بصمة الانصراف
- **الوصف:** الوقت الذي تستطيع فيه المعلمة عمل بصمة الانصراف
- **مثال:** إذا حددت الساعة `14:00`، فإن المعلمة تستطيع بصمة الانصراف من الساعة 2:00 مساءً
- **إذا تركت الحقل فارغاً:** سيتم استخدام الإعداد العام (12:00 ظهراً)

---

## 📝 كيفية إعداد توقيت مخصص لمعلمة

### الخطوات:
1. **افتح صفحة المطور** باستخدام الرابط وكلمة المرور
2. **ابحث عن اسم المعلمة** في القائمة
3. **أدخل الأوقات المخصصة:**
   - فتح بصمة الحضور (مثال: `06:00`)
   - بدء احتساب التأخير (مثال: `08:00`)
   - فتح بصمة الانصراف (مثال: `14:00`)
4. **اضغط على زر "💾 حفظ"**
5. **انتظر رسالة التأكيد:** "✅ تم الحفظ بنجاح!"
6. **تحديث تلقائي:** الصفحة ستُحدَّث تلقائياً بعد 1.5 ثانية

---

## 🔄 إعادة تعيين لإعدادات عامة

إذا أردت حذف التوقيت المخصص والعودة للإعدادات العامة:

1. اضغط على زر **"♻️ إعادة تعيين"** بجانب اسم المعلمة
2. سيظهر تأكيد: "هل أنت متأكد من إعادة التعيين للإعدادات العامة؟"
3. اضغط **"OK"** للتأكيد
4. سيتم مسح جميع الأوقات المخصصة والعودة للإعدادات العامة

---

## 💡 أمثلة عملية

### مثال 1: معلمة بتوقيت صباحي مبكر
**المعلمة:** فاطمة  
**السيناريو:** تبدأ العمل مبكراً في الساعة 6:00 صباحاً

| الإعداد | الوقت المخصص |
|---------|---------------|
| فتح بصمة الحضور | `06:00` |
| بدء احتساب التأخير | `06:15` |
| فتح بصمة الانصراف | `13:00` |

**النتيجة:**
- يمكنها بصمة الحضور من 6:00 صباحاً
- إذا حضرت بعد 6:15 صباحاً، ستُحسب متأخرة
- يمكنها بصمة الانصراف من 1:00 مساءً

### مثال 2: معلمة بتوقيت متأخر قليلاً
**المعلمة:** غالية  
**السيناريو:** تبدأ العمل في الساعة 7:00 صباحاً

| الإعداد | الوقت المخصص |
|---------|---------------|
| فتح بصمة الحضور | `06:30` |
| بدء احتساب التأخير | `07:00` |
| فتح بصمة الانصراف | `14:00` |

**النتيجة:**
- يمكنها بصمة الحضور من 6:30 صباحاً
- إذا حضرت بعد 7:00 صباحاً، ستُحسب متأخرة
- يمكنها بصمة الانصراف من 2:00 مساءً

### مثال 3: معلمة بدون توقيت مخصص
**المعلمة:** شهد  
**السيناريو:** استخدام الإعدادات العامة

| الإعداد | الوقت |
|---------|-------|
| فتح بصمة الحضور | 02:00 (عام) |
| بدء احتساب التأخير | 07:15 (عام) |
| فتح بصمة الانصراف | 12:00 (عام) |

**النتيجة:**
- سيتم تطبيق الإعدادات العامة لجميع المعلمات الأخريات

---

## 🔒 الأمان

### تدابير الأمان المطبقة:
1. **كلمة مرور محمية:** الصفحة مقفلة بكلمة مرور
2. **وصول محدود:** فقط المطور لديه صلاحية الوصول
3. **Cookie آمن:** كلمة المرور تُحفظ في Cookie لمدة 24 ساعة فقط
4. **زر تسجيل خروج:** يمكن مسح الـ Cookie في أي وقت

### تسجيل الخروج:
اضغط على زر **"🚪 تسجيل خروج"** في أعلى الصفحة لمسح كلمة المرور من Cookie

---

## 🗄️ البيانات التقنية

### الأعمدة المضافة في قاعدة البيانات:
```sql
-- جدول teachers
ALTER TABLE teachers ADD COLUMN custom_checkin_start TEXT DEFAULT NULL;
ALTER TABLE teachers ADD COLUMN custom_checkout_start TEXT DEFAULT NULL;
ALTER TABLE teachers ADD COLUMN custom_late_calculation_start TEXT DEFAULT NULL;
```

### API Endpoint للحفظ:
```
POST /api/developer/save-teacher-schedule
Content-Type: application/json

{
  "teacher_id": 3,
  "custom_checkin_start": "06:00",
  "custom_late_calculation_start": "08:00",
  "custom_checkout_start": "14:00"
}
```

### الاستجابة:
```json
{
  "success": true,
  "message": "تم حفظ الأوقات بنجاح",
  "teacher": {
    "id": 3,
    "name": "فاطمه مصطفى السويدان",
    "custom_checkin_start": "06:00",
    "custom_late_calculation_start": "08:00",
    "custom_checkout_start": "14:00"
  }
}
```

---

## 🎨 واجهة المستخدم

### المؤشرات البصرية:
- **🔵 عام:** يظهر تحت الحقل الفارغ (استخدام الإعداد العام)
- **🟢 مخصص:** يظهر تحت الحقل المملوء (استخدام توقيت مخصص)

### الألوان:
- **أزرق:** للحقول الافتراضية (بصمة الحضور)
- **أخضر:** لحقول التأخير
- **بنفسجي:** لحقول الانصراف

---

## 📊 التطبيق العملي

### كيف يعمل النظام في صفحة البصمة؟
1. **عند محاولة بصمة الحضور:**
   - النظام يتحقق من `custom_checkin_start` للمعلمة
   - إذا كان `NULL`، يستخدم `fingerprint_window_start` من الإعدادات العامة
   - يمنع البصمة إذا كان الوقت الحالي أقل من الوقت المحدد

2. **عند احتساب التأخير:**
   - النظام يتحقق من `custom_late_calculation_start` للمعلمة
   - إذا كان `NULL`، يستخدم `work_start_time` من الإعدادات العامة
   - يحسب دقائق التأخير بناءً على الفرق بين وقت البصمة والوقت المحدد

3. **عند محاولة بصمة الانصراف:**
   - النظام يتحقق من `custom_checkout_start` للمعلمة
   - إذا كان `NULL`، يستخدم القيمة الافتراضية (`12:00`)
   - يمنع البصمة إذا كان الوقت الحالي أقل من الوقت المحدد

---

## 🚀 التحديثات المستقبلية المقترحة

### إضافات محتملة:
1. **سجل التعديلات:** حفظ من قام بتعديل أوقات المعلمات ومتى
2. **إشعارات:** إرسال إشعار للمعلمة عند تغيير أوقاتها
3. **نطاق زمني:** إمكانية تحديد نطاق (من - إلى) بدلاً من وقت واحد
4. **أيام العمل:** تحديد أيام عمل مخصصة لكل معلمة

---

## 📞 الدعم الفني

في حالة وجود أي مشاكل أو استفسارات:
- **الإصدار:** 3.1.0
- **تاريخ الإطلاق:** 6 ديسمبر 2025
- **الحالة:** ✅ مفعّل ويعمل بكفاءة

---

## ✅ اختبار الميزة

### خطوات الاختبار:
1. افتح صفحة المطور
2. اختر معلمة للاختبار
3. حدد أوقات مخصصة
4. احفظ التغييرات
5. افتح صفحة البصمة للمعلمة
6. جرب بصمة الحضور في أوقات مختلفة
7. تحقق من رسائل النجاح/الخطأ

---

**تم إنشاء هذا الدليل بواسطة:** نظام إدارة الحضانة  
**الإصدار:** 3.1.0 - Custom Teacher Schedules  
**التاريخ:** 6 ديسمبر 2025
